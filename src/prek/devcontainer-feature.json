{
  "id": "prek",
  "version": "0.1.1",
  "name": "prek",
  "description": "Installs prek, a pre-commit and pre-push hook runner.",
  "options": {
    "version": {
      "type": "string",
      "proposals": [
        "latest",
        "0"
      ],
      "default": "latest",
      "description": "Version of prek to install. Use 'latest' for the newest available."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
